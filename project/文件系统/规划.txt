1.like-redis

提供cache文件系统的能力，兼容redis协议，另外增加ISCSI SMB协议，抽象出对应的协议支援能力，提供高并发高IO性能的文件系统服务。

2.ISCSIClient SMBClient
使用异数OS来提供并发访问文件的能力。

前景：
相对单机本地文件系统，异数OS提供的文件IO能力预计达到500W IO以上，性能提升50倍以上，在并发文件访问容量上，提升100倍，更有利于在分布式环境中提供重压力的文件访问服务。
